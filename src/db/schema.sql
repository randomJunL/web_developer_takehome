CREATE TABLE REQUESTER (
    FirstName varchar(255) NOT NULL,
    LastName varchar(255) NOT NULL,
    Email varchar(255) NOT NULL,
    PRIMARY KEY (Email)
);

INSERT INTO REQUESTER VALUES('Pamela', 'Whitten', 'pamelawhitten@iu.edu' );